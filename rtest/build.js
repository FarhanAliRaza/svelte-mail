import { render } from '@react-email/render';

// We can't dynamically import a .tsx file in Node.js without transpilation
// This file should be run with 'pnpx tsx build.tsx' instead
console.log('Please use: pnpx tsx build.tsx');
console.log('JSX syntax is not supported in plain .js files without transpilation.');